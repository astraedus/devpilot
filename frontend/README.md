# DevPilot Frontend

Next.js 14 dashboard for DevPilot (to be implemented).

## Planned features
- Recent PR reviews list with issue breakdown
- CI incident feed with root cause cards
- Live status polling via /api/reviews and /api/incidents

## Setup (when ready)
```bash
npx create-next-app@14 . --typescript --tailwind --app
```

Backend API base URL: http://localhost:8000
